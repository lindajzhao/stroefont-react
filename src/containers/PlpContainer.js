import React from 'react';
import { ProductGrid } from '../components/ProductGrid'

export const PlpContainer = (props) => {
    return (
        <React.Fragment>
            <div>This is the PLP Container</div>
            <ProductGrid />
        </React.Fragment>
    );
}